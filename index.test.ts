import { KlarnaPayments } from "./index";

describe("klarna-payments-typescript-sdk", () => {
    it("initialize client", async () => {
        const klarnapayments = new KlarnaPayments({
        });
    });
});
